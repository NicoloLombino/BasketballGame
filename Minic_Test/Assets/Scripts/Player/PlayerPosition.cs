using UnityEngine;

/// <summary>
/// the class of position where the player move after a shot
/// need this to check if theposition is full so the player
/// can avoid this and go to next position
/// (Avoid collisions between the players)
/// </summary>
public class PlayerPosition : MonoBehaviour
{
    public bool isFull;
}
